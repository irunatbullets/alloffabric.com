---
label: clash
hide_body: true
fields:
- name: blah_blah
  type: blocks
  label: blah blah
  template_types:
  - welcome
  config:
    min: 1
    max: 1
- name: hannah
  type: blocks
  label: Hannah
  template_types: []
  config:
    min: 
    max: 
pages:
- content/blah.md
